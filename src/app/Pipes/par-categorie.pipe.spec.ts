import { ParCategoriePipe } from './par-categorie.pipe';

describe('ParCategoriePipe', () => {
  it('create an instance', () => {
    const pipe = new ParCategoriePipe();
    expect(pipe).toBeTruthy();
  });
});
